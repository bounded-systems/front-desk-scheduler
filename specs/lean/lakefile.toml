name = "FrontDesk"
defaultTargets = ["FrontDesk", "Leases", "Delegation"]

# Pure-core proofs: no mathlib dependency, they build from the toolchain alone.
[[lean_lib]]
name = "FrontDesk"
srcDir = "."

# S1 (mutual exclusion) — the lease cell vs. the predicate-guarded log.
[[lean_lib]]
name = "Leases"
srcDir = "."

# The credential chain (D1-D3 under C1/C2) — why authority only flows down it.
[[lean_lib]]
name = "Delegation"
srcDir = "."
