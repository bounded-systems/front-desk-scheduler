{
  "name": "@bounded-systems/front-desk-scheduler",
  "version": "0.0.0",
  "exports": {
    ".": "./src/index.ts",
    "./verbs": "./src/verbs.ts"
  },
  "// imports": [
    "`hono` is a LOAN, not a dependency — added 2026-08-03 for #118 and meant to",
    "be deleted. Nothing in this repo imports it. It arrives transitively via",
    "@bounded-systems/verbspec-mcp → @modelcontextprotocol/sdk 1.30.0 (hono",
    "^4.11.4) and @hono/node-server 2.0.12 (peer hono ^4), and both ranges admit",
    "the fixed version — but a plain resolve lands on 4.12.33, leaving",
    "GHSA-8j4g-w8fx-2239 (Moderate 5.3) open. This entry forces >=4.12.34.",
    "",
    "DELETE IT FROM 2026-08-04T02:36:40Z. NOTHING UPSTREAM HAS TO MOVE FIRST —",
    "an earlier draft of this note said the loan waits on verbspec-mcp's own",
    "remediation, which contradicts the paragraph above: both parent ranges",
    "ALREADY admit 4.12.34. The age gate was the entire constraint, so the loan",
    "is repayable the moment 4.12.34 turns 24h old, independent of any upstream",
    "release. The check is one command: remove this entry, `rm deno.lock && deno",
    "install`, and grep the lock for `hono@`. If it comes back >=4.12.34 the loan",
    "is repaid; if lower, put the entry back. Verified 2026-08-03 by resolving at",
    "the exact future boundary (`--min-dep-age 2026-08-03T02:44:00+00:00`, which",
    "reproduces what a post-gate run sees): plain resolve reached 4.12.34 with no",
    "deno.json entry at all, 307/307 green. Tracked on #118.",
    "",
    "A pin on a package you do not import is a claim with a date on it, the same",
    "posture CLAUDE.md takes toward a stale `needs:` — so it carries its own",
    "expiry condition rather than becoming permanent by neglect.",
    "",
    "HOW THE LOCK WAS GENERATED, because a plain re-resolve before 2026-08-04",
    "02:36Z FAILS: 4.12.34 was published 02:36Z on 2026-08-03 and Deno's minimum",
    "dependency age policy (24h default) blocks it until then. The lock here was",
    "resolved with `--min-dep-age 1170` (19.5h) — the SMALLEST relaxation that",
    "admits 4.12.34 and nothing else. Not `0`: with the gate fully off the",
    "resolver took hono 4.13.0, published nineteen minutes earlier, plus an",
    "unrelated jose bump — adopting a minutes-old minor while claiming to fix a",
    "vulnerability is the exact risk the gate exists to stop. `deno install",
    "--frozen`, which is what CI runs, validates against this lock and never",
    "re-resolves, so no flag is needed anywhere after this. After 02:36Z the",
    "flag is unnecessary too — a plain resolve reaches 4.12.34 on its own."
  ],
  "imports": {
    "@bounded-systems/verbspec": "jsr:@bounded-systems/verbspec@^0.4.0",
    "@bounded-systems/verbspec-mcp": "jsr:@bounded-systems/verbspec-mcp@^0.1.0",
    "hono": "npm:hono@^4.12.34",
    "zod": "npm:zod@^4.4.3"
  },
  "// exclude": [
    "wrangler dev writes generated bundles under worker/lease/.wrangler/tmp.",
    "They are gitignored, but `deno check` walks them anyway and dies on them —",
    "so running scripts/lease-integration.sh once would break the type gate in",
    "your working tree while CI (a fresh checkout) stayed green."
  ],
  "exclude": ["**/.wrangler"],
  "tasks": {
    "fds": "deno run -A scripts/fds.ts",
    "mcp": "deno run -A scripts/mcp.ts",
    "compile": "deno compile -A --no-check --output dist/fds scripts/fds.ts"
  },
  "nodeModulesDir": "auto"
}
