node_modules/
dist/
*.log
# TLA+ tooling artifacts
specs/tla/states/
specs/tla/*.toolbox/
*.tla.old
MC*.tla
MC*.cfg

node_modules/
package-lock.json

# TLC tooling
.tools/
specs/tla/states/
specs/tla/*_TTrace_*.tla
specs/tla/*_TTrace_*.bin

# Lean build artifacts
specs/lean/.lake/
specs/lean/build/

# Rust build artifacts
specs/rust/target/

# Dolt mirror (a database, versioned by Dolt itself — not by git)
mirror/

# python venv
.venv/
__pycache__/

# deno compile output
dist/

# wrangler dev local state (workerd + miniflare persistence; lease-integration
# runs `wrangler dev`, which writes here — never commit a local DO namespace)
.wrangler/
